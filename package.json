{
  "name": "moneta",
  "version": "1.0.0",
  "description": "MONETA — Анатомски вложки",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "echo 'No build required for static site'"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "mammoth": "^1.12.0",
    "sharp": "^0.35.3"
  }
}
